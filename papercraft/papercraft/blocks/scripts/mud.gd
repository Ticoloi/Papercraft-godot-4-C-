extends Block

func _ready() -> void:
	pass
	#El block transorma la cobble en dirt
	#var coords: Vector2i =  get_block_position()
	#if(get_world().get_block(Vector2(coords.x, coords.y - 1)) != null):
		#pass
		#if(get_block_at(Vector2(coords.x, coords.y - 1)).get_id() == "stone"):
			#pass
			#get_world().set_block(Vector2(coords.x, coords.y - 1), World.DIRT)
